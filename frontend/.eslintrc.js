module.exports = {
    extends: [
      'eslint:recommended',
      'plugin:react/recommended',
      'plugin:tailwindcss/recommended', // Add the Tailwind plugin
    ],
    plugins: ['tailwindcss'], // Add Tailwind as a plugin
    rules: {
      // Your custom rules (if needed)
    },
  }
  